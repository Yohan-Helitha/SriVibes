{
  "name": "sl-transit-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "echo \"Add lint step\""
  },
  "devDependencies": {
    "turbo": "^1.12.5"
  },
  "dependencies": {
    "@babel/runtime": "^7.28.4"
  }
}
